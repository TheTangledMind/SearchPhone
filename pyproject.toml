[project]
name = "searchphone"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "colorama>=0.4.6",
    "fpdf2>=2.7.0",
    "phonenumbers>=8.13.0",
    "python-dotenv>=1.0.0",
    "requests>=2.31.0",
]

[project.scripts]
searchphone = "searchphone:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
